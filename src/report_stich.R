#support script to take the output of the reports and stich together in to one file. 

#get dir from command line arg

#go to PDFs for each sample

	#combine pdfs in latex format 

#get html for each sample
	
	#create one page that is a index or master page that includes all the different HTML documents on there

#get csv for each sample 

	#create a csv with multiple pages (might need to do in python...
